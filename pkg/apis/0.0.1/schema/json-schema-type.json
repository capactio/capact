{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "#/properties/spec/properties/jsonSchema",
  "type": "object",
  "title": "The JSONSchema definition.",
  "description": "The JSONSchema definition.",
  "properties": {
    "ref": {
      "type": "object",
      "title": "The ref schema",
      "description": "An explanation about the purpose of this instance.",
      "required": [
        "name",
        "revision"
      ],
      "properties": {
        "name": {
          "description": "Reference to OCF Type for example, cap.type.db.mysql.config",
          "type": "string"
        },
        "revision": {
          "type": "string"
        }
      }
    },
    "value": {
      "type": "string",
      "title": "Inline JSON Schema definition for the parameters.",
      "description": "Inline JSON Schema definition for the parameters."
    }
  },
  "additionalProperties": false,
  "oneOf": [
    {
      "required": [
        "ref"
      ]
    },
    {
      "required": [
        "value"
      ]
    }
  ]
}

command: "apply"
module:
  name: rds
  source: "./cmd/terraform-runner/aws/rds.tgz"
variables: |+
  user_name = "voltron"
  user_password = "12-43i213j}"
env: []
output:
  goTemplate:
    host: "{{ .instance_ip_addr }}"
    port: 5432
    defaultDBName: postgres
    endpoint: "{{ .endpoint }}"
    superuser:
      username: "{{ .username }}"
      password: "{{ .username }}"

name: Create release

# When a tagged version commit is pushed, then this workflow creates GitHub release
# and publishes the binaries.

jobs:
  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    environment: Release
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:      
          fetch-depth: 0

ocfVersion: 0.0.1
revision: 0.1.0
kind: Implementation
metadata:
  prefix: cap.implementation.voltron.e2e
  name: passing
  license:
    name: "Apache 2.0"
  displayName: Passing Voltron test action
  description: ""
  documentationURL: ""
  maintainers:
    - email: team-dev@projectvoltron.dev
      name: Voltron Dev Team
      url: https://projectvoltron.dev

spec:
  appVersion: "1.x.x"

  implements:
    - path: cap.interface.voltron.e2e.passing-local # TODO remove -local, after rendering is implemented in engine
      revision: 0.1.0

  requires:
    cap.core.type.platform:
      oneOf:
        - name: kubernetes
          revision: 0.1.0

  imports:
    - interfaceGroupPath: cap.interface.runner.argo
      alias: argo
      methods:
        - name: run
          revision: 0.1.0

  action:
    runnerInterface: argo.run
    args:
      workflow:
        entrypoint: helm
        templates:
          - name: main
            steps:
              - - name: say-hello
                  template: say-hello
          - name: say-hello
            container:
              image: docker/whalesay:latest
              command: ["sh", "-c"]
              args: ["cowsay hello && sleep 2"]

signature:
  och: eyJ0eXAiOiJKV1QiLA0KICJhbGciOiJIUzI1NiJ9

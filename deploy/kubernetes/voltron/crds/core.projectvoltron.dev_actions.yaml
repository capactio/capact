
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: actions.core.projectvoltron.dev
spec:
  group: core.projectvoltron.dev
  names:
    kind: Action
    listKind: ActionList
    plural: actions
    singular: action
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: Action is the Schema for the actions API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ActionSpec describes user intention to resolve & execute a
            given Interface or Implementation.
          properties:
            advancedRendering:
              properties:
                enabled:
                  type: boolean
                renderingIteration:
                  properties:
                    inputArtifacts:
                      items:
                        properties:
                          alias:
                            type: string
                          typeInstanceID:
                            type: string
                          typePath:
                            type: string
                        type: object
                      type: array
                  type: object
              type: object
            inputRef:
              description: InputRef contains reference to resource with Action input.
              properties:
                secretRef:
                  description: LocalObjectReference contains enough information to
                    let you locate the referenced object inside the same namespace.
                  properties:
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        TODO: Add other useful fields. apiVersion, kind, uid?'
                      type: string
                  type: object
              type: object
            path:
              description: Full path for the Implementation or Interface manifest
              type: string
            renderedActionOverride:
              description: RawMessage is a raw encoded JSON value. It implements Marshaler
                and Unmarshaler and can be used to delay JSON decoding or precompute
                a JSON encoding.
              format: byte
              type: string
            run:
              type: boolean
          type: object
        status:
          description: ActionStatus defines the observed state of Action.
          properties:
            advancedRendering:
              properties:
                renderingIteration:
                  properties:
                    inputArtifactsToProvide:
                      items:
                        properties:
                          alias:
                            type: string
                          typeInstanceID:
                            type: string
                          typePath:
                            type: string
                        type: object
                      type: array
                  type: object
              type: object
            builtInRunner:
              description: BuiltinRunnerStatus holds data related to built-in Runner
                that runs the Action.
              properties:
                interface:
                  description: Interface is a full path of built-in Runner Interface
                    manifest.
                  type: string
                status:
                  description: StatusRef contains reference to resource with built-in
                    Runner status data.
                  format: byte
                  type: string
              type: object
            cancelledBy:
              description: UserInfo holds the information about the user needed to
                implement the user.Info interface.
              properties:
                extra:
                  additionalProperties:
                    description: ExtraValue masks the value so protobuf can generate
                    items:
                      type: string
                    type: array
                  description: Any additional information provided by the authenticator.
                  type: object
                groups:
                  description: The names of groups this user is a part of.
                  items:
                    type: string
                  type: array
                uid:
                  description: A unique value that identifies this user across time.
                    If this user is deleted and another user by the same name is added,
                    they will have different UIDs.
                  type: string
                username:
                  description: The name that uniquely identifies this user among all
                    active users.
                  type: string
              type: object
            condition:
              type: string
            createdBy:
              description: UserInfo holds the information about the user needed to
                implement the user.Info interface.
              properties:
                extra:
                  additionalProperties:
                    description: ExtraValue masks the value so protobuf can generate
                    items:
                      type: string
                    type: array
                  description: Any additional information provided by the authenticator.
                  type: object
                groups:
                  description: The names of groups this user is a part of.
                  items:
                    type: string
                  type: array
                uid:
                  description: A unique value that identifies this user across time.
                    If this user is deleted and another user by the same name is added,
                    they will have different UIDs.
                  type: string
                username:
                  description: The name that uniquely identifies this user among all
                    active users.
                  type: string
              type: object
            message:
              type: string
            outputRef:
              description: OutputRef contains reference to resource with Action output.
              properties:
                secretRef:
                  description: LocalObjectReference contains enough information to
                    let you locate the referenced object inside the same namespace.
                  properties:
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        TODO: Add other useful fields. apiVersion, kind, uid?'
                      type: string
                  type: object
              type: object
            renderedAction:
              description: RawMessage is a raw encoded JSON value. It implements Marshaler
                and Unmarshaler and can be used to delay JSON decoding or precompute
                a JSON encoding.
              format: byte
              type: string
            runBy:
              description: UserInfo holds the information about the user needed to
                implement the user.Info interface.
              properties:
                extra:
                  additionalProperties:
                    description: ExtraValue masks the value so protobuf can generate
                    items:
                      type: string
                    type: array
                  description: Any additional information provided by the authenticator.
                  type: object
                groups:
                  description: The names of groups this user is a part of.
                  items:
                    type: string
                  type: array
                uid:
                  description: A unique value that identifies this user across time.
                    If this user is deleted and another user by the same name is added,
                    they will have different UIDs.
                  type: string
                username:
                  description: The name that uniquely identifies this user among all
                    active users.
                  type: string
              type: object
          required:
          - condition
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []

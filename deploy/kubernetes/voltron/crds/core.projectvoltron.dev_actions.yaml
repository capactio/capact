
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: actions.core.projectvoltron.dev
spec:
  additionalPrinterColumns:
  - JSONPath: .spec.path
    description: Interface/Implementation path of the Action
    name: Path
    type: string
  - JSONPath: .spec.run
    description: If the Action is approved to run
    name: Run
    type: boolean
  - JSONPath: .status.phase
    description: Status of the Action
    name: Status
    type: string
  - JSONPath: .metadata.creationTimestamp
    description: When the Action was created
    format: date-time
    name: Age
    type: date
  group: core.projectvoltron.dev
  names:
    kind: Action
    listKind: ActionList
    plural: actions
    shortNames:
    - ac
    singular: action
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: Action describes user intention to resolve & execute a given Interface
        or Implementation.
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ActionSpec contains configuration properties for a given Action
            to execute.
          properties:
            advancedRendering:
              description: AdvancedRendering holds properties related to Action advanced
                rendering mode.
              properties:
                enabled:
                  default: false
                  description: Enabled specifies if the advanced rendering mode is
                    enabled.
                  type: boolean
                renderingIteration:
                  description: RenderingIteration holds properties for rendering iteration
                    in advanced rendering mode.
                  properties:
                    continue:
                      default: false
                      description: Continue specifies the user intention to continue
                        rendering using the provided InputArtifacts. As the input
                        artifacts are optional, user may continue rendering with empty
                        list of InputArtifacts.
                      type: boolean
                    inputArtifacts:
                      description: InputArtifacts contains Input Artifacts passed
                        for current rendering iteration.
                      items:
                        description: InputArtifact holds input artifact reference,
                          which is in fact TypeInstance.
                        properties:
                          alias:
                            description: Alias refers to input artifact name used
                              in rendered Action.
                            type: string
                          typeInstanceID:
                            description: TypeInstanceID is a unique identifier for
                              the TypeInstance used as input artifact.
                            type: string
                        required:
                        - alias
                        - typeInstanceID
                        type: object
                      type: array
                  required:
                  - continue
                  type: object
              required:
              - enabled
              type: object
            cancel:
              default: false
              description: Cancel specifies whether the Action execution should be
                cancelled.
              type: boolean
            inputRef:
              description: InputRef contains reference to resource with Action input.
              properties:
                secretRef:
                  description: SecretRef stores reference to Secret in the same namespace
                    the Action CR is created.
                  properties:
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        TODO: Add other useful fields. apiVersion, kind, uid?'
                      type: string
                  type: object
              required:
              - secretRef
              type: object
            path:
              description: Path contains full path for Implementation or Interface
                manifest.
              minLength: 3
              type: string
            renderedActionOverride:
              description: RenderedActionOverride contains optional rendered Action
                that overrides the one rendered by Engine.
              format: byte
              type: string
            run:
              default: false
              description: Run specifies whether the Action is approved to be executed.
                Engine won't execute fully rendered Action until the field is set
                to `true`. If the Action is not fully rendered, and this field is
                set to `true`, Engine executes a given Action instantly after it is
                resolved.
              type: boolean
          required:
          - path
          type: object
        status:
          description: ActionStatus defines the observed state of Action.
          properties:
            advancedRendering:
              description: AdvancedRendering describes status related to advanced
                rendering mode.
              properties:
                renderingIteration:
                  description: RenderingIteration describes status related to current
                    rendering iteration.
                  properties:
                    inputArtifactsToProvide:
                      description: InputArtifactsToProvide describes which input artifacts
                        might be provided in a given rendering iteration.
                      items:
                        description: InputArtifact holds input artifact reference,
                          which is in fact TypeInstance.
                        properties:
                          alias:
                            description: Alias refers to input artifact name used
                              in rendered Action.
                            type: string
                          typeInstanceID:
                            description: TypeInstanceID is a unique identifier for
                              the TypeInstance used as input artifact.
                            type: string
                        required:
                        - alias
                        - typeInstanceID
                        type: object
                      type: array
                  type: object
              type: object
            builtInRunner:
              description: Runner holds data related to Runner that runs the Action.
              properties:
                interface:
                  description: Interface is a full path of Runner Interface manifest.
                  minLength: 3
                  type: string
                status:
                  description: StatusRef contains reference to resource with arbitrary
                    Runner status data.
                  format: byte
                  type: string
              required:
              - interface
              type: object
            cancelledBy:
              description: CancelledBy holds user data which cancelled a given Action.
              properties:
                extra:
                  additionalProperties:
                    description: ExtraValue masks the value so protobuf can generate
                    items:
                      type: string
                    type: array
                  description: Any additional information provided by the authenticator.
                  type: object
                groups:
                  description: The names of groups this user is a part of.
                  items:
                    type: string
                  type: array
                uid:
                  description: A unique value that identifies this user across time.
                    If this user is deleted and another user by the same name is added,
                    they will have different UIDs.
                  type: string
                username:
                  description: The name that uniquely identifies this user among all
                    active users.
                  type: string
              type: object
            createdBy:
              description: CreatedBy holds user data which created a given Action.
              properties:
                extra:
                  additionalProperties:
                    description: ExtraValue masks the value so protobuf can generate
                    items:
                      type: string
                    type: array
                  description: Any additional information provided by the authenticator.
                  type: object
                groups:
                  description: The names of groups this user is a part of.
                  items:
                    type: string
                  type: array
                uid:
                  description: A unique value that identifies this user across time.
                    If this user is deleted and another user by the same name is added,
                    they will have different UIDs.
                  type: string
                username:
                  description: The name that uniquely identifies this user among all
                    active users.
                  type: string
              type: object
            message:
              description: Message provides a readable description of the Action phase.
              type: string
            outputRef:
              description: OutputRef contains reference to resource with Action output.
              properties:
                secretRef:
                  description: SecretRef stores reference to Secret in the same namespace
                    the Action CR is created.
                  properties:
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        TODO: Add other useful fields. apiVersion, kind, uid?'
                      type: string
                  type: object
              required:
              - secretRef
              type: object
            phase:
              default: Initial
              description: ActionPhase describes in which state is the Action to execute.
              enum:
              - Initial
              - BeingRendered
              - AdvancedModeRenderingIteration
              - ReadyToRun
              - Running
              - BeingCancelled
              - Cancelled
              - Succeeded
              - Failed
              type: string
            renderedAction:
              description: RenderedAction contains partially or fully rendered Action
                to be executed.
              format: byte
              type: string
            runBy:
              description: RunBy holds user data which run a given Action.
              properties:
                extra:
                  additionalProperties:
                    description: ExtraValue masks the value so protobuf can generate
                    items:
                      type: string
                    type: array
                  description: Any additional information provided by the authenticator.
                  type: object
                groups:
                  description: The names of groups this user is a part of.
                  items:
                    type: string
                  type: array
                uid:
                  description: A unique value that identifies this user across time.
                    If this user is deleted and another user by the same name is added,
                    they will have different UIDs.
                  type: string
                username:
                  description: The name that uniquely identifies this user among all
                    active users.
                  type: string
              type: object
          required:
          - phase
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "engine.fullname" . }}-cluster-policy
  labels:
  {{- include "engine.labels" . | nindent 4 }}
data:
  cluster-policy.yaml: |
    apiVersion: 0.1.0

    rules:
      {{- toYaml .Values.clusterPolicyRules | nindent 8 }}


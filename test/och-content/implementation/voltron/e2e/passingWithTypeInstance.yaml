ocfVersion: 0.0.1
revision: 0.1.0
kind: Implementation
metadata:
  prefix: cap.implementation.voltron.e2e
  name: passingWithTypeInstance
  license:
    name: "Apache 2.0"
  displayName: Passing Voltron test action
  description: ""
  documentationURL: ""
  maintainers:
    - email: team-dev@projectvoltron.dev
      name: Voltron Dev Team
      url: https://projectvoltron.dev

spec:
  appVersion: "1.x.x"

  additionalInput:
    typeInstances:
      e2e:
        typeRef:
          path: cap.type.e2e.test
          revision: 0.1.0
        verbs: [ "get" ]

  implements:
    - path: cap.interface.voltron.e2e.passingWithTypeInstance
      revision: 0.1.0

  requires:
    cap.core.type.platform:
      oneOf:
        - name: kubernetes
          revision: 0.1.0

  imports:
    - interfaceGroupPath: cap.interface.runner.argo
      alias: argo
      methods:
        - name: run
          revision: 0.1.0

  action:
    runnerInterface: argo.run
    args:
      workflow:
        entrypoint: main
        templates:
          - name: main
            steps:
              - - name: cat
                  template: cat
                  arguments:
                    artifacts:
                      - name: e2e
                        from: "{{ workflow.outputs.artifacts.e2e }}"
          - name: cat
            inputs:
              artifacts:
                - name: e2e
                  path: /e2e
            container:
              image: alpine:latest
              command: ["sh", "-c"]
              args: ["sleep 2 && cat /e2e"]
            

signature:
  och: eyJ0eXAiOiJKV1QiLA0KICJhbGciOiJIUzI1NiJ9
